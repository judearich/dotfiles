# TODO list
1. Make dein and plugins work in vim AND neovim
2. Have dein use XDG_DATA_DIR instead of .local/share
3. Set fortune to only run if present
4. Create bash config for systems which only have bash
